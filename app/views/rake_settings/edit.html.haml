%h2 Edit Rake Setting
- form_for(@rake_setting, :url => channel_rake_setting_path(@account, @channel), :method => "put") do |f|
  = f.label "Percentage"
  %br
  = f.text_field :percentage
  %br
  %br
  = f.label "Start Date"
  %br
  = f.text_field :start_date
  %br
  %br
  = f.submit "Edit Date"
