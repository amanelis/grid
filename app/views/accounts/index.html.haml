%header
  - if @current_user.admin? || @current_user.group_user?
    %ul.right.strong
      %li= link_to image_tag("icons/account.png"), new_account_path, :rel => "facebox", :title => "Add Account"
  %h1 Accounts
%article
  %form{:href => '/', :action => "accounts", :id => 'index', :name => 'index', :method => 'GET'}
    %label Status
    %select{:id => :account_status, :name => :account_status}
      - for status in @accounts_statuses
        %option{ :selected => status == @passed_status}= status
    %input{:type => 'submit', :value => 'Sort'}
  %label.quiet= (Date.yesterday.beginning_of_month).to_s(:long) + ' to ' + Date.yesterday.to_s(:long)
  %table
    %thead
      %tr
        %th Account
        %th Leads
    %tbody
      - @accounts.each do |account|
        %tr
          %td= link_to account.name, account_path(account)
          -account_data = @accounts_data[account.id]
          -account_type = account.account_type
          %td= account_data.present? ? account_data[:leads] : 0
  -#= link_to "Create New Account", "#", :class => "button"