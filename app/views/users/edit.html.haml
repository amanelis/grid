%header
  %h1=@user.email
%article
  - form_for (@user) do |f|
    %h2 Login Information
    = f.label "Email"
    %br
    = f.text_field :email, :class => 'title'
    %br
    = f.label "Password"
    %br
    = f.password_field :password, :class => 'title'
    %br
    = f.label "Confirm Password"
    %br
    = f.password_field :password_confirmation, :class => 'title'
    = f.hidden_field :id, :value => @user.id
    %br
    = f.submit "Save", :class => 'button'

  %h2 Roles
  %h3.quiet
    Currently:
    %span
      -@user.roles.each do |role|
        -if role.role_type_type == "GroupUser"
          =role.role_type_type.to_s + ": " + role.role_type.group_account.name
        -elsif role.role_type_type == "AccountUser"
          =role.role_type_type.to_s + ": " + role.role_type.account.name.to_s
        %br
      -if @user.admin?
        ="Admin"
  -form_for(@user) do |f|
    = f.radio_button :admin, 'Admin', :checked => (@user.admin? ? true : false)
    %label Admin
    %br
    = f.radio_button :group_users, 'Group User', :checked => (@user.group_user? ? true : false)
    %label Account Manager
    %br
    = f.radio_button :account_users, 'User', :checked => (@user.account_user? ? true : false)
    %label User